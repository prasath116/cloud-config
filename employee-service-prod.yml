server:
  port: 8063

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8061/eureka/
      
logging:
  pattern: 
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"

springdoc:
  packagesToScan: com.prs.services

spring:
  data:
    mongodb:
      uri: mongodb+srv://prasathm:$prasathm@cluster0.awbzd.mongodb.net/University?retryWrites=true&w=majority